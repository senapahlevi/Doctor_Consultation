import React from 'react'
import {View} from 'react-native'
/*
ini fungsi sebagai gap jarak kayak di menu awal 
get staarted dan sign in 
*/
const Gap = ({height, width}) => {
    return <View style={{height:height, width:width}} />;
};

export default Gap;

